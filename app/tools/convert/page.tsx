"use client";
// Stub pages for remaining tools — each will be replaced with full implementation

export default function ConvertPage() {
  return (
    <div style={{ padding: "32px" }}>
      <h1 style={{ color: "white", fontSize: "24px" }}>Convert PDF</h1>
      <p style={{ color: "rgba(255,255,255,0.4)", marginTop: "8px" }}>
        Conversion tool — server-side LibreOffice backend required for Office formats.
        See OMNIPDF_MASTER_PLAN.md for full implementation details.
      </p>
    </div>
  );
}
